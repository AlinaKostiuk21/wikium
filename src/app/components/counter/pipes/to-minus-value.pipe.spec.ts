import { ToMinusValuePipe } from './to-minus-value.pipe';

describe('ToMinusValuePipe', () => {
  it('create an instance', () => {
    const pipe = new ToMinusValuePipe();
    expect(pipe).toBeTruthy();
  });
});
